// Set the global ADMIN_PIN for the frontend
window.ADMIN_PIN = '3004';  // Default PIN for local development

// Optional: debug log to confirm it loaded
console.log("window.ADMIN_PIN injected:", window.ADMIN_PIN);
